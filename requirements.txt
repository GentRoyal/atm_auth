fastapi==0.111.0
uvicorn[standard]==0.29.0
python-dotenv==1.0.1
asyncpg
databases[asyncpg]
sqlalchemy==2.0.30
bcrypt==4.1.3
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
pydantic
pydantic-settings

# Vercel deployment stack.
# Voice auth is disabled on Vercel. Face verification uses the Pillow/NumPy
# fallback in backend/services/face_service.py to stay within Vercel limits.
numpy==1.26.4
Pillow

# HTTP / SMS
twilio

# Utils
python-multipart
qrcode
